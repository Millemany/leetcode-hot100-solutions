# leetcode 279 完全平方数

这是一个完全背包问题。

本题相当于有一个容积为 n 的背包，以及体积为$`(1^2, 2^2,3^2,\cdots)`$的物品，每个物品有无限个。求从这些物品里挑出最少多少件能填满背包。

因为最小的物品体积是 1，所以一定有答案。

用`f[v, i]`表示用前`i`个物品（也就是体积从$`1^2`$到$`i^2`$）填满容积为`v`的背包的最少物品数。根据完全背包问题的性质，有
```math
f(v, i) = \min(f(v, i - 1), f(v-i^2, i) + 1)
```
$`f(v, i - 1)`$表示不选用第 i 个物品时候的最小物品数。$`f(v-i^2,i) + 1`$表示选用第 i 个物品时候的最小物品数，这里使用了递归的表示方法。

时间复杂度是$`O(n\sqrt n)`$，因为一共有$`\sqrt n`$个物品。

---

本题可以使用滚动数组优化空间。因为计算$`f(\cdot, i)`$只会用到$`f(\cdot, i-1)`$的值，所以用一个长度为$`n + 1`$的数组，维护截至目前填充各体积所用的最小物品数就可以了。

因此代码结束后，答案就是`f[n]`。

---

下面说一下初始化的问题。初始化$`f(v)`$表示用 0 个物品填充容积为$`v`$的背包，最少要多少个物品。因此有$`f(0) = 0`$。而当 $`v`$ 不为 0 的时候，是根本不可能实现的，因此$`f(v)`$应当是无穷大。

而实际上，我们知道$`f(v,i)`$永远不会大于$`f(n, 1) = n`$，因此把$`f(v)(v\ne0)`$初始化成$`n + 1`$就可以了。

```c++
class Solution {
public:
    int numSquares(int n) {
        vector<int> f(n + 1, n + 1);
        f[0] = 0;

        for(int i = 1; i * i <= n; i ++){
            int x = i * i;
            for(int v = x; v <= n; v ++){
                f[v] = min(f[v], f[v - x] + 1);
            }
        }

        return f[n];
    }
};
```

