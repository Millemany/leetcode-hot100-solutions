# leetcode 4 寻找两个正序数组的中位数

本题可以做如下考虑。令`int m = nums1.size(), n = nums2.size()`。

从数学上可以证明，如果`m + n`是奇数，那么按序合并后第`(m + n + 1) / 2`个数字就是中位数。如果`m + n`是偶数，那么第`(m + n + 1) / 2`个数字与其下一个数字的平均值就是中位数。因此我们要找的就是合并后数组的第`(m + n + 1) / 2`个数字。

---

最直接的想法就是用两个指针分别定位两个数组的开头，哪个指针指向的值更小，就让它前进一步。每前进一步，就是跳过了一个当前最小的数字。那么只要前进`(m + n + 1) / 2`步就可以了。

但这种方法时间复杂度是$`O(m+n)`$级别。我们需要更快的方法。用二分可以实现更快的方法。

也就是说，前进`(m + n + 1) / 2`步时两个指针的位置可以不用遍历找到，而用二分方法找到。

---

实际上这`(m + n + 1) / 2`个数字，就是取`nums1`中前`i`个数，再取`nums2`中前`(m + n + 1) / 2 - i`个数得到的。因此我们可以考虑对`i`进行二分。

我们令`l = 0`，表示取`nums1`中前若干个数的下限，`r = m`(**注意不是`m - 1`了，因为二分的不再是下标，而是数字个数**)，表示取`nums1`中前若干个数的上限。`mid = (l + r) / 2`。如果取`mid`个数太少，我们就让`l = mid + 1`；如果太多，就让`r = mid - 1`，然后继续迭代。直到`l == r`。

那么怎么判断取`mid`个数是多是少呢？看下面的例子。

假如现在有下面两个数组：

```c++
nums1 = {3, 4, 5, 6};
nums2 = {1, 2, 4, 5, 8, 10};
```

此时`m == 4, n == 6`，我们需要找出`(4 + 6 + 1) / 2 == 5`个最小数字。

我们知道，这五个最小的数字应该是`1, 2, 3, 4, 4`，也就是取`nums1`的前两个元素和`nums2`的前三个元素。如果我们画出分割线的话，就有：

```txt
3 4 | 5 6
1 2 4 | 5 8 10
```

因为这五个数字是两个数组里最小的，所以每一个分割线左边的数字都**小于等于**分割线右边的所有数字。

而因为`nums1`和`nums2`都是有序的，所以一个正确的分割，**当且仅当**

```c++
nums1[i - 1] <= nums2[j] && nums2[j - 1] <= nums1[i] 
```

其中`i`是`nums1`分割数字的数量，`j`是`nums2`分割数字的数量，且满足`i + j == (m + n + 1) / 2`。



如果`i`过小会怎么样呢？

如果`i == 1`，也就是这五个最小数字是由`nums1`的第一个数字和`nums2`的前四个数字组成的，即

```txt
3 | 4 5 6
1 2 4 5 | 8 10
```

我们看到，`nums1`分割线右边的数字是4，`nums2`分割线左边的数字是5。存在了分割线左边的数字大于右边数字的情况。也就是说

```c++
nums2[j - 1] > nums1[i]
```

这样就说明`i`过小了。



`i`过大也是类似的。如果`i == 3`，即

```txt
3 4 5 | 6
1 2 | 4 5 8 10
```

此时`nums1`分割线左边是5，`nums2`分割线右边是4。此时出现

```c++
nums1[i - 1] > nums2[j]
```



我们把上面的`i`代换成`mid`，就可以判定`mid`到底是过大还是过小了。

---

下面是具体代码，时间复杂度是$`O(\log (m + n))`$。

```c++
class Solution {
public:
    double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
        int m = nums1.size(), n = nums2.size();
        if(m > n) return findMedianSortedArrays(nums2, nums1);
        // 我们保证 nums1 的长度不会大于 nums2 的长度，也就是 m <= n
        // 因为我们只想找 (m + n + 1) / 2 个元素
        // 如果 m < = n，那么分割上限 r = m 就没有问题，因为一定有 m <= (m + n + 1) / 2
        // 如果 m > n，那么就有分割上限 r = (m + n + 1) / 2
        // 因此为了避免分类讨论，简化代码书写，直接约定 m <= n

        int l = 0, r = m;
        while(l <= r){
            // 这里循环结束条件是 l <= r，而不是以往的 l < r
            // 因为这次找到答案、计算返回的逻辑也在循环里面写，这样写很方便
            // 因此必须把 l == r 的情况包进循环里
            int mid = (l + r) >> 1;
            int nid = ((m + n + 1) >> 1) - mid; // 分割 nums2 元素的个数

            int lm = mid ? nums1[mid - 1] : INT_MIN;
            int rm = mid == m ? INT_MAX : nums1[mid];
            int ln = nid ? nums2[nid - 1] : INT_MIN;
            int rn = nid == n ? INT_MAX : nums2[nid];
            // lm 就是 nums1 分割线左边的元素，也就是上面说的 nums1[i - 1]
            // rm 就是 nums1 分割线右边的元素，即 nums1[i]
            // ln 和 rn 分别就是 nums2 分割线左右的元素
            
            // 如果 mid == 0，也就是说 nums1 里一个数字都不取
            // 此时分割线左侧就没有元素了，mid - 1 会越界
            // 为了保证代码逻辑的完整，要给 lm 赋一个最小的值，也就是 INT_MIN。
            // rm 也同理，如果 mid == m，也就是全取 nums1 的元素
            // 此时分割线右边也没元素了，rm 就得是 INT_MAX。

            if(lm > rn) r = mid - 1;	// mid 太大了
            else if(ln > rm) l = mid + 1;	// mid 太小了
            else{	// 找到答案了
                // 按照以往的二分写法，这一部分应该写在循环外面，然后循环条件是 while(l < r)
                // 但是这次必须要用到循环体里面定义的变量，因此不如写到循环里面
                if((m + n) % 2){	// 一共有奇数个元素，中位数就是 lm 和 ln 的最小值
                    return double(max(lm, ln));
                }
                else{ // 偶数个元素，就是中间两个元素的平均值
                    double l_all = max(lm, ln);
                    double r_all = min(rm, rn);
                    return (l_all + r_all) / 2; 
                }
            }
        }
        return 0.0;
    }
};
```



