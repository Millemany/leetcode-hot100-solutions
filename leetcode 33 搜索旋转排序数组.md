# leetcode 33 搜索旋转排序数组

在普通二分的基础上增加了下标的转换。

---

首先要做的就是找到旋转前数组的开头。这也可以用二分来解决。

- 如果数组没有旋转，那么开头下标就是`0`
- 如果数组旋转了，那么使用二分找到**第一个小于`nums[0]`的元素位置**，就是旋转前数组开头。

二分不仅可以处理单调性的数据。只要数据满足二段性，也就是数据可以分成两段，前一段一定满足（或不满足）某性质，后一段一定不满足（或满足）某性质就可以了。

因此可以先找到第一个小于`nums[0]`元素的位置`k`，然后判断`nums[k] < nums[0]`是否成立。如果不成立，说明数组根本没有旋转（也就是说`nums[0]`就是数组里最小的元素）。否则`k`就是旋转前的开头

----

下面就可以进行二分。还是用`l = 0, r = n - 1`进行二分。但是数组的真正开头是在下标`k`的地方，结尾是在下标`k - 1`的地方。因此`l`和`r`都是为了方便二分而使用的虚拟下标，需要把它们映射到真实的数组下标。

根据数学知识可知，如果`i`是`nums`某元素的真实下标，`j`是其在二分查找中的虚拟下标，那么它们的关系是：
$$
j = (i +n-k) \mod{n}
$$

$$
i = (j + k)\mod{n}
$$

这样就可以做了。

时间复杂度是$O(\log n)$。

```c++
class Solution {
public:
    int search(vector<int>& nums, int target) {
        int n = nums.size();
        
        int l = 0, r = n - 1;
        while(l < r){
            int mid = (l + r) >> 1;
            if(nums[mid] < nums[0]) r = mid;
            else l = mid + 1;
        }

        int k = nums[r] < nums[0] ? r : 0;  
        // 旋转之前数组的起点
        // 如果 nums[r] >= nums[0] 说明数组根本没有旋转 
        
        l = 0, r = n - 1;
        while(l < r){
            int mid = (l + r) >> 1; // 虚拟下标
            int t_mid = (mid + k) % n;  // 真实下标
            if(nums[t_mid] < target) l = mid + 1;	// 还是用虚拟下标操作
            else r = mid;
        }

        int t_r = (r + k) % n;	// 算出结果的真实下标
        if(nums[t_r] == target) return t_r;
        else return -1; 
    }
};
```





